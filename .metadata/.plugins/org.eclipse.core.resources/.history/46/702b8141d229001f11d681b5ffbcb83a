package entities;

public class Retangulo {
	public double a;
	public double b;
	
	public double area() {
		return a * b;
	}
	
	public double perimeter() {
		return (2 * a) + (2 * b);
	}
	
	public double diagonal() {
		return Math.sqrt(Math.pow(a, 2.0) + Math.pow(b, 2));
	}
}
